<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Appointments</title>
    <link rel="stylesheet" href="/css/my-appointment.css">
</head>
<body>
    <div class="appointments-container">
        <h1>My Appointments</h1>
        
        <% if (appointments.length > 0) { %>
            <% appointments.forEach(appointment => { %>
                <div class="appointment-card">
                    <div class="appointment-header">Appointment</div>
                    <div class="appointment-detail">Date: <span><%= new Date(appointment.date).toDateString() %></span></div>
                    <div class="appointment-detail">Time: <span><%= appointment.time %></span></div>
                    <div class="appointment-detail">Reason: <span><%= appointment.reason %></span></div>
                </div>
            <% }) %>
        <% } else { %>
            <p>No appointments found.</p>
        <% } %>
    </div>
</body>
</html>
